# dotfiles

- vimrc install memo

   ```sh
   $ mkdir -p ~/.vim/bundle
   $ git clone git://github.com/Shougo/neobundle.vim ~/.vim/bundle/neobundle.vim
   ```

- ctags memo
    - Mac OSX

     ```sh
     $ brew install ctags
     ```
